group 'com.viewsonic.flutter_mirror'
version '1.0'

buildscript {
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:7.1.2'
    }
}

rootProject.allprojects {
    repositories {
        google()
        mavenCentral()
        maven {
          url 'https://viewsonic-ssi.pkgs.visualstudio.com/_packaging/myViewBoard_Artifacts/maven/v1'
          name 'myViewBoard_Artifacts'
          authentication {
              basic(BasicAuthentication)
          }
          credentials {
              username project.AZURE_ARTIFACTS_USERNAME
              password project.AZURE_ARTIFACTS_PASSWORD
          }
        }
    }
}

apply plugin: 'com.android.library'

android {
    compileSdkVersion 31
    ndkVersion '23.2.8568313'

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    defaultConfig {
        minSdkVersion 21
        externalNativeBuild {
            cmake {
                abiFilters "armeabi-v7a", "arm64-v8a", "x86_64"
                arguments "-DANDROID_STL=c++_shared"
            }
        }
    }

    externalNativeBuild {
        cmake {
            path file('src/main/cpp/CMakeLists.txt')
            version '3.22.1'
        }
    }
    buildFeatures {
        prefab true
    }
}

dependencies {
    implementation 'com.google.oboe:oboe:1.7.0'
    implementation 'com.viewsonic.airplay:airplay:0.9.8'
    implementation 'com.viewsonic.googlecast:googlecast:0.2.1'
}
