# flutter_ion_sfu

A flutter plugin for ion sfu.

## Development

```
cd ion-sfu-library

# Run the ion sfu server on Android
.\scripts\android\run.cmd

# Run the ion sfu server on Windows
.\scripts\windows\run.cmd

# Build the android aar
.\scripts\android\build-aar.cmd

```
